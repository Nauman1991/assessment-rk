Express js is used

In Controller folder all Controller are stored for e.g user

In Route folder all router are stored for e.g userRouter

All router are register in app.js

In Model folder all model for db are stored

Database use in project is postgres

In bin/www, socket.io is implemented. There is a function which is emiting hard coded notification and settimeout is used

for database you can use sequlise cli to migrate

JWT token is been use, in app.js jwt is use to check if token is correct or not

All error's are handle on backend and frontend as well

Password is secured using crypto and md5 and encrypted password is stored in db

postman collection is in root of backend

Two folder is been used
Frontend : Nextjs
Backend : Nodejs